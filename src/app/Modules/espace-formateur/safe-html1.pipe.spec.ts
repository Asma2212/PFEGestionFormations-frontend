import { SafeHtml1Pipe } from './safe-html1.pipe';

describe('SafeHtml1Pipe', () => {
  it('create an instance', () => {
    const pipe = new SafeHtml1Pipe();
    expect(pipe).toBeTruthy();
  });
});
